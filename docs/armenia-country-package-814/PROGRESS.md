# Armenia country package progress

## State

Issue #814 is in repository-convention and schema reconnaissance. The `am/`
package has not yet been drafted.

## Done

- Read the repository `AGENTS.md` and `CLAUDE.md` operating rules.
- Confirmed that the branch starts from `origin/main` with no existing `am/`
  package changes.
- Started parallel inspection of the Belgium spec-only package, the country-spec
  schema and tests, the New Zealand v1 plan, and the calibration doctrine.
- Moved this journal under `docs/`, following the New Zealand planning-doc
  convention. Country directories are closed spec inventories: in-package
  Markdown would make `am/` fail both its loader and spec-only tests.

## Next

1. Finish the required source and design reading.
2. Draft the engine-free Armenia pool-reweight specifications and harvest notes.
3. Add golden coverage and run the spec-only and shard test suites.
4. Record final test evidence and maintainer questions here and in the requested
   final output report.
