# Bounded completion-host corrections — 2026-09-13

Source corrections are complete within the requested 20-minute window beginning
22:09 UTC. **All tests, collection, project imports and runtime acceptance remain
UNRUN.** This section supersedes the earlier implementation report preserved below.
No runtime, native-capacity, scientific, release, default-adoption or PR893 change
is claimed. No commit or staging occurred: the later explicit prohibition governs
the standing commit instruction. PROGRESS.md was maintained without committing.

## Provenance and ownership

Worktree: `/Users/maxghenis/PolicyEngine/_worktrees/microcosm-us-completion-host-integration-20260913`.
Unchanged HEAD: `6aa745c8762c3e4a140b8168470de5827e12c5cb`.
The dirty shared worktree was intentional. This pass edited exactly:

- `packages/microcosm-build/src/microcosm/build/us_runtime/graph_survey_completion_host.py`
- `packages/microcosm-build/tests/test_us_graph_atomic_completion_host.py`
- `docs/us-survey-completion-host.md`
- `PROGRESS.md`
- `FINAL_REPORT.md`

Read CLAUDE.md and the full independent review and full approved R2 plan at the
exact supplied recovered paths under
`/Users/maxghenis/PolicyEngine/_recovered/scratch-backup/893/codex-us-followup-host-integration-20260913/`.
The review's plan-location objection resulted from its search scope; the supplied
absolute plan exists and its complete file hash matches the approved hash.

| Input | SHA-256, unchanged at final source check |
| --- | --- |
| `HOST-INTEGRATION-PLAN-R2.md` | `b402d6be9d374774e27da7bf42fb83a216305d4cddd2ad8347c8149536269a29` |
| `HOST-SOURCE-INDEPENDENT-REVIEW.md` | `f98a9927e882b3f788eaebf55863d150023efd467a9bc2fc57770225ec5c5a4e` |
| Shared `graph_atomic_survey_financial.py` | `7545d1315b978735b8da4e02ad10ad78ce3d72854e2cc31a607bbd073210fb84` |
| Frozen `graph_survey_completion.py` | `858b4f42d9d61596901797976bfb2a9bf9bca9f9820b930fb3484e3f641ef0e1` |
| Frozen `graph_property_tax_leaves.py` | `883afb4a95652dd664d636b3a2eaf8481e18f4c43e3d7a24ae0cfb51b6b68c93` |

Earlier review/failure packets were not changed. Shared source, other tests,
inventory, CI and changelog files were not edited. A new dirty entry for
`test_us_spine_blindness.py` appeared during the pass; it is another owner's work
and was neither inspected nor changed here. No reset, clean, rebase, commit,
staging, project imports, collection/tests, project scripts, dependency resolution,
network/browser/agents, native/private/installed payloads, or execution-evidence
containers were used. Source reads, metadata/hashes, standard-library AST parsing,
direct Ruff and whitespace checks were the inspection/validation operations.

## Corrections and source evidence

1. **Fragment identity/counts (S1):** exact ordered Node IDs now bind receiving1,
   optional roles0/2, child6 and tax3 before compilation. Receiving and role
   stages also check before their intermediate compilation. The child document
   count derives from retained nodes. Direct boundary controls use real retained
   declarations: moving an actual tax node into child7 while reducing tax to2
   preserves the total but refuses; wrong receiving identity and reversed roles
   refuse too. No issuer, executor or compiler is replaced.
2. **Knownness provenance (S2):** the portable flag derives from the child
   verification bytes produced by `_child_expected` reconstruction. The existing
   materialized child verifier and exact payload comparison remain mandatory
   before issuance. The document hashes those same bytes. The positive assertion
   binds the flag and hash to the retained payload and issued artifact hashes.
   Arbitrary JSON flags acquire no authority.
3. **Roles disabled (T1):** separate test selectors author actual cold/required
   49-node status-enabled and 45-node status-disabled host runs. Each explicitly
   selects the partial ACS+ASEC fixture (2/3 selection, seed41) with the full-source
   teenage donor outside the selected ASEC household. They assert actual checked
   views, 39/35-node retained bases, no role fragment or writes, exact incumbent
   role preservation/absence, final completeness and declaration/manifest/payload/
   artifact parity, staged base hits, extension misses on cold and all required
   hits. The 51-node adverse suite is not multiplied.
4. **Child adversaries/copying (T2/T6):** exact full refusal strings are
   `CHILD_PROPERTY_RETAINED_VALUES_CHANGED`,
   `CHILD_PROPERTY_RECONSTRUCTED_ARTIFACT_CHANGED` and
   `CHILD_PROPERTY_MATERIALIZED_VERIFICATION`. Source ordering is
   `verify_materialized` evidence seal -> `_current`/`_pure` -> reconstruction ->
   `_artifacts` -> `_verification_value`; the parent mutation fails in `_pure`,
   artifact bytes at reconstruction comparison, and producer/key mismatch at
   `_verification_value`. Revocation and original-base checks remain. The comment
   now states verification can reconstruct/refit. Executor `_apply_result` FILTER
   calls `Frame.select`; `Frame.__init__` copies every table. Role `_patch_columns`
   calls `_copied_tables(deep=True)`. The test additionally asserts the actual
   base stamp is unchanged immediately after mutating the reconstructed parent.
   No counterfeit issuer or replacement parent is used to avoid this cost.
5. **Ordering and conservation (T3):** assertions cover all actual prefix9 nodes,
   financial/property attachment and enabled status BIND before completion
   receiving. Both actual observed FILTER transitions must preserve entity
   dimensions, complete tables, schema, strata and weights and append precisely
   one ledger record. Source uses separate `operation="filter"` and
   `policy="conserve"` fields, not a `filter_conserve` operation token. Total,
   stratum and partition mass must match. Actual owners/versions are checked from
   property/status writers to completion FILTER, optional role BIND, child ATTACH,
   tax FILTER and final tax-leaf writer. No pseudo-node or generic API change.
6. **Authentic identity controls (T4/T5):** the existing issued tax-rebased51 run
   refuses as a second completion parent. The 49-node warm handle undergoes a
   same-values/different-Population-identity substitution; restoration cannot undo
   revocation and its base/cold peer remain checked. Extracted private union
   observation and final-I/O checks remain in the production `_extend` path.
   Tests reuse actual observed51 evidence to refuse a base miss (even though the
   manifest content key stays equal), a missing observation and changed retained
   source/node/implementation/artifact identities. The final-I/O checker re-reads
   the real source/store and implementations, without executing kernels or adding
   a fit. No fake runner/owner is used. Other original final-I/O population-mutation
   controls remain distinct from these exact final-identity refusals.

The fixture's source-construction wrappers now restore before issuance so later
fixture composition cannot recursively wrap an earlier fixture's closure, replace
its new literals, or reuse the earlier restoration output directory. Source pins
remain under the existing real fixture's monkeypatch lifetime. This is authored
source logic, not demonstrated fixture execution.

S3's empty-ordering generic suggestion is not a live host defect: the host supplies
two typed property edges and the completion FILTER follows the full cloned spine.
Frozen receiving/tax APIs and compiler/source-blindness guards remain unchanged.
The independently confirmed old status+tax42 complement-owner defect is unrelated;
its isolated successor remains root-owned. This pass does not change shared
`graph_atomic_survey_financial.py`, broaden accepted owners, or certify that path.

## Authored selectors and finite scope — all UNRUN

AST only: **16 test functions / 32 source-expanded cases**, up from10/20. Counts
come from literal parameter-list lengths; no pytest collection was performed.
Prefix every selector below with
`packages/microcosm-build/tests/test_us_graph_atomic_completion_host.py::`.

| Selector | Cases |
| --- | ---: |
| `test_refused_options_precede_source_io` | 6 |
| `test_roles_disabled_status_enabled_49_cold_required_and_output_identity` | 1 |
| `test_roles_disabled_status_disabled_45_cold_required` | 1 |
| `test_disabled_signature_and_serialized_option_parity` | 1 |
| `test_exact_51_roster_order_and_actual_required_hits` | 1 |
| `test_actual_donor_outside_selection_and_paired_child_draws` | 1 |
| `test_full_weights_geography_status_roles_and_complete_gate` | 1 |
| `test_detached_parent_refuses_without_owner_substitution` | 1 |
| `test_actual_tax_rebased_parent_refuses` | 1 |
| `test_wrong_fragment_roster_refuses_before_compile` | 3 |
| `test_actual_union_observation_and_base_cache_refusals` | 2 |
| `test_actual_union_final_io_identity_refusals_without_execution` | 4 |
| `test_explicit_scientific_identity_changes_declarations` | 3 |
| `test_incomplete_adult_refuses_actual_puf_qualification_before_fitting` | 1 |
| `test_real_child_verifier_refuses_changed_custody` | 3 |
| `test_final_source_io_mutation_permanently_revokes_host` | 2 |

The 49 and45 selectors each construct their own pair and are selectable without
the shared51 fixture. The complete file authors seven top-level host constructions:
three cold/required pairs plus one incomplete-adult run. Each host stages a base
and extension; seven is not a graph-execution count or a fit budget. Repeated
checked views and child materialized verification can requalify/reconstruct/refit.
The four final-identity parameters each perform one positive and one adverse real
I/O check. Root must budget these reads and reconstructions explicitly. Final
population-poisoning controls remain last on their shared handles.

## Source checks and unresolved scope

- Standard-library `ast.parse` passed for both owned Python files; selector counts
  were obtained from their AST without importing project code.
- Direct `/opt/homebrew/bin/ruff check` passed for both owned Python files.
- Direct `/opt/homebrew/bin/ruff format --check` passed: both files formatted.
- `git diff --check` returned0 without findings. Individual untracked source/test/
  doc checks used `git diff --no-index --check /dev/null PATH`; each reported no
  whitespace diagnostics and returned the no-index difference status1.
- No runtime failure/success evidence was created. Some source-location searches
  missed guessed filenames and were resolved with `rg`; no execution containers
  were opened. Direct Ruff found no lint failures in this correction pass.

| Remaining scope | Concrete reason and next action |
| --- | --- |
| T4 financial-only parent (`property_income=None`) | No authentic19/23 financial-only run is retained in this fixture. Author/budget a separate real issuance; do not substitute a forged state. The tax-rebased-parent branch is now covered by source-authored test. |
| T5 initial base-key/artifact and final manifest/declaration adversaries | Final source/key/implementation/artifact identity and observed/base-hit controls are authored, but not every distinct earlier/later refusal is provoked. Exact additional boundaries need authentic retained evidence and separate finite scope; do not fake executor/compiler output. |
| T5 role reconciliation and union compiler roster | A conflicting original-source role needs coherent pre-issuance source ownership, not post-issuance erasure. Exact fragments now prevent compensating counts; a compiler-corruption control would require prohibited substitution or a separately reviewed authentic construction. |
| T6 fixture/checked-view behavior | All new and existing composition assumptions remain UNRUN. Guarded execution must validate source attachment restoration, member pins, selected-child/adult completeness, and actual role/status custody. Source confirms `puf55_survey_recipients.qualify_puf55_survey_recipients` calls `require_complete_property_taxes` before recipient/source admission. |
| T7 unsupported GQ / zero weights / modeled-zero science | Positive sources remove GQ and the upstream helper deliberately requests positive weights. A valid GQ/zero-weight source fixture needs separate source-owner review/changes. The selected positive child draw does not establish modeled-zero scientific coverage. Keep unknowns unknown; no zero-fill or scope waiver. |
| Root integration and resources | Independent combined-source review; source/import-use inventories, test-spine/CI classification, identity/golden reconciliation, status42 repair, generic/country/status gates, fresh guarded stage/resource budget and all runtime acceptance remain outstanding. |

## Exact authored file identities

| File | Preimage SHA-256 | Final SHA-256 |
| --- | --- | --- |
| `graph_survey_completion_host.py` | `6ce066b4e1b33ed1572a5e82a3414070a5f6b7360ee451028634d0862109beac` | `25852246f2690ec754dbc62ae88173f0ba1fb8115d9aa1ea86375f81daf3df40` |
| `test_us_graph_atomic_completion_host.py` | `54678f4758f9b8a96d9dc885e2f22a5fa997b1458033dcafc754075b6f8c9857` | `14735a01e119f853935d04ea574f38f452b230d16717a0ef3ea898548753abae` |
| `docs/us-survey-completion-host.md` | `f59702f85d69298bddd8ccfe2c3e3399267c66daf70b28e348c9312fb187553f` | `d654164704eb728b75c01fa8e02f54fcae6a235164f540626827a266905fc372` |
| `PROGRESS.md` | `8680141c6857cf7a8fde48c625dc47e1fba5a1f868f07f1be09bf7bf96aeeb2f` | `b42bfb5e4de0e13a1edf45229ae857e75cc4032756d21744ecad356873274071` |
| `FINAL_REPORT.md` | `95a74f7d0a49c896e1e4934e998eb7d63166a1103978d3a6fff192491a34100c` | Reported externally in the final response to avoid a self-referential hash. |

## Historical implementation report (preserved)

The following report predates the independent correction pass. Its10/20 counts,
45/49 coverage gaps and earlier verification descriptions are historical; the
correction section above is the current source-only handoff.

# US completion host source report — 2026-09-13

Source implementation is authored for root inspection. **All tests and runtime
checks remain UNRUN. This is not combined-host acceptance.**

Branch: `us-completion-host-integration-20260913`.
Resolved base / unchanged HEAD: `6aa745c8762c3e4a140b8168470de5827e12c5cb`.
Frozen R2 plan verified SHA-256:
`b402d6be9d374774e27da7bf42fb83a216305d4cddd2ad8347c8149536269a29`.
Read the original independent APPROVE_SOURCE_PLAN review and repository instructions.

Implemented explicit `household_roles=False` and `child_property=None`, strict
unsupported-mode refusals and exact explicit ChildPropertyOptions. Disabled
serialization remains conditional. The actual issued 35/39 property parent is
retained with its original preparation, store, keys, registry and legacy/property
outputs. Registries are copied before extension. A privately reconstructed full-row
receiving version, optional reconciled roles, unchanged child6 and separate tax3
compose the union; all issued prefix nodes must hit cache. Tax declarations use
the independently completed Frame and bind the actual child verification edge.

The existing issuer/state/checker retain the full union, all node populations and
states, registry identity and completion custody. Actual completed/support
Populations and typed store evidence reach the real child materialized verifier,
including required replay. Source/store work precedes final pure seals. Failed
completion checks revoke permanently. Old property verification receives its
retained original output. Documentation labels old routing as pre-child source
gaps, preserves modeled-zero semantics and the all-person tax/PUF gate.

| Changed path | SHA-256 |
| --- | --- |
| `packages/microcosm-build/src/microcosm/build/us_runtime/graph_atomic_survey_financial.py` | `7545d1315b978735b8da4e02ad10ad78ce3d72854e2cc31a607bbd073210fb84` |
| `packages/microcosm-build/src/microcosm/build/us_runtime/graph_survey_completion_host.py` | `6ce066b4e1b33ed1572a5e82a3414070a5f6b7360ee451028634d0862109beac` |
| `packages/microcosm-build/tests/test_us_graph_atomic_completion_host.py` | `54678f4758f9b8a96d9dc885e2f22a5fa997b1458033dcafc754075b6f8c9857` |
| `docs/us-survey-completion-host.md` | `f59702f85d69298bddd8ccfe2c3e3399267c66daf70b28e348c9312fb187553f` |
| `changelog.d/us-survey-completion-host.added.md` | `0f16800fb200eca15adcce43d8b44f8a73e6d89c1af79b8c85bfdee00006eef6` |
| `PROGRESS.md` | `8680141c6857cf7a8fde48c625dc47e1fba5a1f868f07f1be09bf7bf96aeeb2f` |

Output file: `FINAL_REPORT.md` (this report). PROGRESS.md was maintained in place.
No commit or push was made: the task-specific prohibition overrides the standing
commit order. No reset, rebase, clean, staging or other-owner edit occurred.

Static verification:
- AST parsing passed for all three owned Python files.
- `_FinancialRunState` has 41 annotated fields and its constructor has 41
  positional arguments; the new state-tail alignment was inspected statically.
- Direct `/opt/homebrew/bin/ruff check` passed for the three owned Python files.
- Direct Ruff format checks passed; only owned files were formatted.
- `git diff --check` passed for the edited tracked host/journal files.
- Exactly **10 test functions / 20 source-expanded cases** authored. No collection
  or tests ran. Positive fixture: one shared cold51/required51 pair. Separate
  incomplete-adult case: one host run and actual PUF qualifier refusal. Three
  adverse child-custody cases reuse actual run evidence with separately constructed
  private boundaries; they do not add graph fits. Final-I/O controls poison the
  two positive handles last. No mocked owner or executor substitutes were added.

Remaining work / missing acceptance seams:
- Independently review the full combined diff and the other agent's receiving/tax
  seam APIs. Their files were read but never edited by this task.
- Execute the new fixture only under root's fresh bounded resource approval.
  Its pre-issuance ACS housing restriction, ASEC donor sampling, coherent role and
  status literals, final member pins and monetary attachment composition are
  authored assumptions, not demonstrated runtime results. No outside-owned
  fixture edit has been established as necessary by source inspection.
- 45/49 counts are source-derived; this test file's full positive execution target
  is 51. Dedicated 45/49 execution controls, unsupported-GQ and zero-weight host
  fixture coverage, and broader identity/adversarial cases remain to be added or
  reconciled with existing component controls by root.
- Root owns graph/source inventories, AST import-use records, test-spine/CI
  classification, identity/golden reconciliation and the exact final closure and
  resource-call budget. None were updated or run here. Existing upstream component
  passes do not certify this composition; generic/country/status/runtime gates
  remain independently required.
- No national-scale/native/PUF-fit, scientific age-transfer, fiscal, release,
  publication or default-adoption claim is made.

Failure evidence: initial direct Ruff runs found only import-order findings in
the new helper and new test file; these were corrected and subsequent checks
passed. Several source-search commands returned missing-path/no-match statuses
while locating maintained modules; no runtime failure evidence was produced or
removed. No project imports, collection, dependency resolution, network/browser/agents,
source staging, preflight, guard, native/private payload access or installed-resource
inspection was performed. Work stayed inside the requested 20-minute source window.
