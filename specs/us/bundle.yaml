# DRAFT (v3) — pseudocode until schemas land.
#
# Round 2 (sol M10 / Pro): country_package.json becomes THE single typed
# resource manifest ({path, kind, schema_id} rows replacing bare
# filenames) — this file no longer carries a second file inventory.
# Bundle-level settings only. Root specs/ is a drafting location; the
# bundle ships as package data through the CountrySpec seam.
country: us
identity_generation: 1
seed_protocol: legacy-v1        # enforced via the RNG broker + draw-site
                                # ledger; derived-v2 is an F3 edit
status: draft-v3-pseudocode
# Emitted (never authored): bundle.lock.json, plan.lock.json,
# engine_abi.lock.json. Generated locks are reproducible from their
# authorities and rejected if hand-edited.
# Identity: run_provenance_identity (records) vs node_reuse_key (reuse) —
# see docs/spec-engine.md "Identity". The release line in publication is
# PROVISIONAL/non-normative pending D6 (excluded from spec_sha256).
