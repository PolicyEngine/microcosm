# DRAFT (v3) — pseudocode until the loader binds. D6 RULED (Max 2026-08-16).
attempts:
  model: append_only_events_then_terminal_seal
  terminal_states: [landed, failed, expired]
promotion:
  latest_flip: human_gate            # unchanged doctrine
  idempotency: required_key
  recovery: [seal_ok_append_fail, append_ok_alias_fail,
             orphan_reconciliation, expiry_reconciliation]
release:
  line:
    value: microcosm-us-2024         # D6: the spec-engine-era line
    normative: true                  # hashed into spec_sha256
    legacy_prefixes: [populace-us-2024]   # readers accept; rows valid-historical
    note: "HF dataset destination is NOT part of D6 — unchanged until a separate explicit ruling."
  pattern: "{line}-stacked-f{rung}-s{seed}"
  rungs: [f001, f004, f010, f025, f100]
audit_chain:
  kind: strict_linear
  store: supabase:logbook            # OPERATIONAL binding; never hashed
release_graph:
  relations: [derived_from, supersedes, revokes]
