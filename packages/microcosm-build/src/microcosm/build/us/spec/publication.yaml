# GENERATED by tools/generate_us_bundle_from_constants.py; migration tombstone 2026-11-16.
attempts:
  model: append_only_events_then_terminal_seal
  terminal_states:
  - landed
  - failed
  - expired
promotion:
  latest_flip: human_gate
  idempotency: required_key
  recovery:
  - seal_ok_append_fail
  - append_ok_alias_fail
  - orphan_reconciliation
  - expiry_reconciliation
release:
  line:
    value: microcosm-us-2024
    normative: true
    legacy_prefixes:
    - populace-us-2024
    note: HF dataset destination is NOT part of D6 — unchanged until a separate explicit ruling.
  pattern: '{line}-stacked-{rung}-s{seed}-asec{asec_households}-acs{acs_households}-{timestamp}-{nonce}'
  rung_fractions:
  - fraction: 0.01
    token: f001
    percent_basis_points: 100
  - fraction: 0.04
    token: f004
    percent_basis_points: 400
  - fraction: 0.1
    token: f010
    percent_basis_points: 1000
  - fraction: 0.25
    token: f025
    percent_basis_points: 2500
  - fraction: 1.0
    token: f100
    percent_basis_points: 10000
audit_chain:
  kind: strict_linear
  store: supabase:logbook
release_graph:
  relations:
  - derived_from
  - supersedes
  - revokes
