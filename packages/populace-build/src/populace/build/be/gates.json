{
  "version": 1,
  "country": "be",
  "policy": "Belgium has no incumbent dataset, so the incumbent-comparison gates (parity, export_surface, target_surface) are deliberately not selected: external oracles replace self-parity (EUROMOD-BE baselines and Federal Planning Bureau reform scores, populace#264). National and NUTS1 targets hard-gate; commune-grain rows are diagnostic until the spine demonstrably supports them.",
  "gates": [
    {
      "id": "calibration_per_family_fit",
      "gate": "per_family_fit",
      "criticality": "release_blocking",
      "parameters": {
        "within": 0.1,
        "min_family_share": 0.8
      },
      "notes": "Calibration fit per source family (demography, fiscal income, income tax, social security, caseloads) so no family hides in the average."
    },
    {
      "id": "national_and_nuts1_admin_aggregates",
      "gate": "aggregate_admin",
      "criticality": "release_blocking",
      "parameters": {
        "geography_levels": ["country", "nuts1"],
        "default_rtol": 0.05
      },
      "notes": "Weighted aggregates against admin anchors on the hard surface. Criticality tiers per target arrive with the Ledger profile (PolicyEngine/ledger#70): e.g. PIT total tighter than caseloads."
    },
    {
      "id": "commune_fiscal_income_fit",
      "gate": "aggregate_admin",
      "criticality": "diagnostic",
      "parameters": {
        "geography_levels": ["commune"],
        "report_worst_fit": 25
      },
      "notes": "Commune-grain diagnostics render per-target fit with the worst-fit communes named; promotion to release_blocking is a spine-quality decision (populace#263), not a default."
    },
    {
      "id": "target_profile_coverage",
      "gate": "target_profile_coverage",
      "criticality": "release_blocking",
      "parameters": {
        "required_families": [
          "demography",
          "fiscal_income",
          "income_tax",
          "social_security",
          "caseloads"
        ]
      },
      "notes": "The active target profile must cover every named family; a build that silently dropped a family must not release."
    },
    {
      "id": "demographics_vs_statbel",
      "gate": "aggregate_admin",
      "criticality": "release_blocking",
      "parameters": {
        "surface": "demography",
        "default_rtol": 0.02
      },
      "notes": "The US demographics.json / base_population_scale posture against Statbel population structure: age-band x sex x region totals within tight tolerance."
    },
    {
      "id": "exported_nonzero",
      "gate": "exported_nonzero",
      "criticality": "release_blocking",
      "parameters": {},
      "notes": "No all-zero stored layers: an all-zero column is a pipeline bug or dead scaffolding masking engine defaults."
    },
    {
      "id": "formula_owned_export",
      "gate": "formula_owned_export",
      "criticality": "release_blocking",
      "parameters": {},
      "notes": "No Axiom derived-rule output may persist as a dataset input (a stored engine output masks reforms) — enforced from day one; the adapter's writer refuses such columns and this gate re-checks the artifact."
    },
    {
      "id": "nonnegative_columns",
      "gate": "nonnegative_columns",
      "criticality": "release_blocking",
      "parameters": {},
      "notes": "Physically non-negative columns (incomes, contributions) carry no negatives; the column list follows the source manifest's nonnegative_outputs."
    },
    {
      "id": "macro_realism_nbb",
      "gate": "macro_realism",
      "criticality": "release_blocking",
      "parameters": {
        "bands": {
          "household_disposable_income_vs_nbb": [0.7, 1.1]
        }
      },
      "notes": "Broad release-blocking band against the NBB household-sector anchor — a levels sanity check, not a calibration target (the populace#212 lesson)."
    }
  ]
}
