[project]
name = "populace-frame"
version = "0.1.0"
description = "The populace kernel: the Frame (weighted sampling frame of entity tables), typed weights, strata, links, weighted accounting, unit structure, and the rules-engine protocol"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2",
    "pandas>=2.3",
]

[project.optional-dependencies]
us = ["microunit>=0.1.0"]
policyengine = ["policyengine-us>=1.0", "microunit>=0.1.0"]

[dependency-groups]
dev = [
    "pytest>=8",
    "microunit>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/populace"]
