Split concentrated CI test groups and the full installed-wheel inventory into four parallel file shards per Python version, preserving module fixtures, environment coverage and process isolation, and fail before pytest when file selection fails or is empty.
