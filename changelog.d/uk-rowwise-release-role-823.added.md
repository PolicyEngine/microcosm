`tools/build_uk_rowwise_candidate.py` now requires `--release-role {national,dense}`: a frozen posture record (`uk_runtime/rowwise_posture.py`) supplies each role's solve defaults, gate scope, Logbook pipeline, staging ids, release id and output filenames, the manifest (schema 4) records `release_role` and `release_id`, and each role refuses the other's flags. The dense pre-flight and assembler accept only a `dense` manifest; the national role is declared and validated here and gains its build flow in the next increment. The driver's default telemetry upload cadence is now 300 s, and a refused stage event is reported and dropped instead of aborting the build.
