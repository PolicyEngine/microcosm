US release seams now refuse a stored model input the certified engine does not define (microcosm#1026). policyengine-us ignores a stored column it has no variable for, so `populace-us-2024-spm-20260915` and its reported-receipt child, both certified against policyengine-us 2.2.1, shipped the WIC take-up draw as `would_claim_wic` (renamed `takes_up_wic_if_eligible`) and every WIC-eligible person took WIC up. `microcosm.data.stored_inputs` refuses any stored column that is lowercase snake_case (the engine's naming convention, measured against 2.2.1), is not a variable of the certified engine, and is not in `US_STORED_NON_VARIABLE_COLUMNS`, a reviewed register of 49 provenance, source-lineage and build-construction columns, each with its reason and bound by test to the code that produces it. Every refused column is named with its tables and both remedies: rename it to its live input, or add a reviewed register entry. Three seams run it, each with the engine the release records as built-with. The US fiscal-refresh release tool, which the exact-k ladder lane also runs, applies it as a batched pre-export gate and grades the written H5, which must earn the same verdict (and refuse nothing if the gate could not evaluate). The source-enrichment native-loader probe applies it before either loader, both at certification and when validation or the publisher replays the contract; the probe's receipt records the register's digest. The ACS local-area chain's package stage applies it before it assembles the release directory and records the verdict in the gate summary. The published default, its reported-receipt child and the 2026-09-23 ACS local-area release built on that child are each refused for two retired engine inputs: `would_claim_wic`, and `medicare_part_b_premiums`, the ASEC Part B premium transfer target, which policyengine-us versions 1.452.0 through at least 1.670.2 defined as an input and which 1.690.7 had replaced with `medicare_part_b_premiums_reported`. #590 already dropped the Part B transfer from the build. The Route A rehearsal export built from main and the Build Q stacked multispine pool pass. `python -m microcosm.data.stored_inputs <h5>...` prints the verdict for local files from HDF metadata alone.
