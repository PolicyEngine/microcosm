The eCPS parity release gate grades the pinned reference's retired layer names under the live engine names, using the same rename register as the release input-coverage manifest (`REFERENCE_ECPS_LAYER_RENAMES`). On policyengine-us 2.2.1 the gate compared the reference's `would_claim_wic` against a candidate that carries the input as `takes_up_wic_if_eligible` (renamed in PolicyEngine-US 1.777.0) and refused a populated layer as all-zero. The projection is recorded in the gate details and refuses to merge two layers.
