Run the US target materializer's base simulation over the same household batches as its JCT reform loop, releasing each engine before the next batch. Guard base and reform engines against known periods of the two Medicaid SLCSP state aggregates and the household and SPM-unit income deciles, including nested and detached temporary branches. Head-to-head slices and ACS chunks arm the same guard; aggregate-reaching targets require the whole pool unbatched in one slice or chunk. Check group nesting and require positive stored Medicaid claiming-tax-unit IDs to resolve within the person's household before slicing. The watched list is pinned by an engine-source pattern scan, not a proof of household locality (microcosm#956). Record batch counts separately from the guard's armed status and checked-variable list, which remain in published head-to-head compilation records and their digests. Keep checkpoint materializer version 13 above microcosm#1018's version 12, so earlier checkpoint identities and their reform caches miss.
