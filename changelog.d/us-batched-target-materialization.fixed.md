Run the US target materializer's base simulation over the same household batches as its JCT reform loop, releasing each engine before the next batch. Batch-size invariance requires household-local engine values: multi-batch runs now refuse known periods of the two Medicaid SLCSP state aggregates and the household and SPM-unit income deciles, including dependencies reached through downstream targets such as `medicaid_cost`. Such targets require one whole-pool batch. Check group nesting across the entire pool before building a batch engine. Record batch counts in the compilation receipt, and omit that size-dependent receipt from head-to-head slice contract digests. Advance the checkpoint materializer version to 13, above PR-2's reserved version 12, so earlier checkpoints and their identity-bound reform caches miss.
