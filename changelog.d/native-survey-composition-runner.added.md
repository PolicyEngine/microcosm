Add `tools/run_us_native_survey_composition.py`, a guarded development runner for the native US survey composition: the financial host with the reviewed development profile, the PUF55 extension and the survey enrichment run over one live owner inside one source verification epoch, then the development checkpoint is written and read back. It enforces memory/disk admission, CPU/wall/RSS ceilings, a system-memory floor, and no network, subprocess or country-engine use, and writes an aggregate-only receipt. It is not a build, calibration or release path.
