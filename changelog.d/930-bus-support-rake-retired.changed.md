The ETB `bus_subsidy_spending` rake to the published net support is retired (microcosm#930; María's ruling of 2026-09-23 that raking retires with the imputation). A declared `price_bus_support` step (`uk_runtime/bus_support_pricing.py`) sets each household's bus support from the frame's journeys the way fares now are: the published concessionary travel reimbursement per concessionary boarding times its eligible persons' boardings plus the rest of the published net support per boarding times every boarding, for London and England outside London (DfT BUS05bi and BUS01) and Scotland (Scottish Transport Statistics components and journeys); Wales (no published boardings) and Northern Ireland (the NITHC accounts carry bus and rail jointly) keep the chain's raw draw, clipped to donor support, as Wales fares do. The support clip no longer exempts the column; the published totals stay bound as calibration targets and now measure the priced quantity. The `rake_to_vendored_facts` operation kind, `uk_runtime/fact_raking.py` and the `fact_rake` stage-health check leave with their last consumer; `uk_stage_etb_services_support_pricing` (`bus_support_pricing`, a population fact check) recomputes every area's components and per-boarding rates from the vendored rows. `dft_bus_journeys.json` and `devolved_bus_finance.json` name the new consumer.
