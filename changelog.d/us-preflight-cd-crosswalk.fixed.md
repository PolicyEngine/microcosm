The US release-gate preflight compiles the Ledger target surface through the same congressional-district vintage crosswalk as the release tool (the packaged one unless `--congressional-district-vintage-crosswalk` replaces it) and records which crosswalk it used. Before this, a feed whose SOI district facts arrive in an earlier district vintage (the pinned c5e5bf8 feed lists West Virginia's three pre-2022 districts) failed the preview's registry compile before any check ran.
