Count two-child-limit child targets as counts rather than household
indicators, attribute band edges to their own groupby dimension, make the
measure-provider fence answer only for routes it can take, put calibration
attempts on the ratified `uk/frs` Logbook chain with failure rows and unique
attempt ids, seal the verified Ledger identity into the run receipts, refuse
operator-supplied measure exclusions on release candidates, and let the rule-1
scorer materialize and authenticate what it scores.
