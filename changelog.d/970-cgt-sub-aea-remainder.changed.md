UK CGT amounts stage (`hmrc_cgt_gains_spine`): gainers beyond the published taxpayer mass now take amounts from the Advani-Summers within-band gains distribution restricted to (0, annual exempt amount], rank-preserving within each total-income band and deterministic, instead of being capped at exactly the exempt amount. The shared Advani-Summers surface (loader, band lookup, degree-1 quantile spline and its inverse) moves to `uk_runtime/advani_summers.py`; the stage receipt carries a `remainder` block the stage-health gate checks (microcosm#970).
