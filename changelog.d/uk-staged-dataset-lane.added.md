Add the staged-dataset lane: the finished rowwise bundle (every manifest-registered output, the manifest, `staged_manifest.json`, `sha256sums.txt`) is uploaded in one commit to `staged/<run_id>/` of the private UK dataset repository, keyed by the telemetry run id, with `staged_dataset` evidence in the manifest and reviewed `staged_dataset`/`fit_summary` artifacts in the telemetry run; `--staged-dataset-repo-id`, `--no-staged-dataset`, `tools/stage_uk_rowwise_candidate.py` and `tools/fetch_uk_staged_dataset.py`. A remote stage is refused up front without a Hub credential that can write the repository (read tokens and fine-grained tokens scoped elsewhere included); epoch telemetry is thinned to a bounded row count for any `--epochs`. Staging is not publication: `releases/` and `latest.json` are untouched.
