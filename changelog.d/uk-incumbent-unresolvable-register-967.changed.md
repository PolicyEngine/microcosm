Pruning a target from both arms of the UK rule-1 evaluation now stands on a reviewed register, `uk/incumbent_unresolvable_measures.json` (reviewed-exclusion schema, keyed `entity.variable`): the scorer prunes a measure only under a signed, in-force entry and refuses an unlisted one, the receipt records the register digest and the entries each pruned row stands on, and the release-cut certifier re-checks every pruned measure against the committed register. Skip rows are matched to the failing measure exactly (`'entity.variable'`), never by substring. The six measures the enhanced FRS is known not to carry (the four administrative-basis UC family inputs, the ONS household type, the CGT asset type) are signed by María on 2026-09-22, in force to 2026-12-22.
