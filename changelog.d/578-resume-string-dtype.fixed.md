Canonicalize semantic strings across every registered pool entity at checkpoint, ACS-target-bank decode, and H5 publication boundaries, so fresh and resumed multispine builds round-trip identically without invalidating banked v2 checkpoint bytes. Reject untyped all-missing object columns at the lossy pool-H5 encoder before they can acquire a guessed string dtype.
