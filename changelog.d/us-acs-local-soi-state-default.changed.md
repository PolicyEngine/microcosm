Make `state` the default SOI target surface of the US ACS local-area chain (`tools/build_us_acs_local_release.py --soi-mode`, previously `full`): every state-geography `irs_soi` spec outside the congressional-district file, which reproduces the 4,459-target contract Build O and Build P were calibrated to (on the pinned feed, 102 SNAP + 51 Medicaid + 3,819 SOI admin specs from the three TY2022 Historic Table 2 state tables, plus 487 population marginals). `totals` (ACA premium tax credit rows only; no state AGI, income-tax or EITC total) and `full` (adds the TY2023 congressional-district file; too large for a dense matrix on one machine) are explicit opt-ins. Unknown modes are refused, the materialized mode is recorded in the build manifest (`materialize.soi_mode`) and the release refresh recipe, and a checkpoint that records no known mode is refused at package. The national release is unchanged.
