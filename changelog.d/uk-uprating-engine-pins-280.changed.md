The declared UK uprating appliers read the engine's index values from a vendored pin file (`uk/hmrc_uprating_engine_pins.json`: six parameters at every 1 January 2019–2026, written from policyengine-uk 2.100.0 by `tools/pin_uk_uprating_engine_values.py`), so the UK target compile runs and reproduces without policyengine-uk; a `requires_uk` test holds the pins in lockstep with the installed engine and a `--check` mode refuses drift.
