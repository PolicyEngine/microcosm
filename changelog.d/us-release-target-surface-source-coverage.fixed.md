Under `--target-surface national_state`, `us_source_coverage.json` no longer lists the congressional-district sources (`census-acs-s0101-congressional-district-age-2024`, `soi-congressional-district-2022`) as active calibration coverage. It records them as reviewed exclusions that name the surface drop and its count, so the artifact agrees with `build.target_surface_selection`. The default `full` surface is unchanged.
