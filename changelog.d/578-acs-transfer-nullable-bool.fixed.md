Accept nullable object-backed boolean donor targets in ACS transfer, restore
the measured hours-worked producer to the pool chain, and explicitly receipt
SCF/SIPP asset inputs deferred beyond the raw-only pool stage.
