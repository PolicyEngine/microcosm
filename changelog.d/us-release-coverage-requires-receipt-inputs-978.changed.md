Require the three ASEC reported-receipt inputs (`receives_wic`, `receives_snap`, `receives_tanf`) in the US release input-coverage gate and its manifest, with no reviewed exclusion, so a national release cannot ship without the columns the ACS local-area transfer needs in its donor (#978 option 1). The manifest consistency check rejects demoting any of the three. The published `populace-us-2024-spm-20260915` default carries none of them and now fails the gate on all three, in addition to the `takes_up_wic_if_eligible` failure it already had.
