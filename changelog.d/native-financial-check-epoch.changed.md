Reuse authenticated preparation validation within one native financial run check. Every independent check still fully validates its sources at entry and close; checks inside an existing verification epoch join that scope, whose close must succeed before the caller returns. Direct Frame seals and source-change refusals remain enabled. Changes invisible to the memo signature may be refused at the enclosing close instead of an intervening borrow.
