The ETB `bus_subsidy_spending` rake to the published net support (kept, per the #930 plan's default) is now fenced at stage time and measured against a value-side alternative (microcosm#930 C7, the #890 support-side leftover). A `fact_rake` stage-health check (`uk_stage_etb_services_support_rake`, a population fact check) recomputes every declared cell's published value from the vendored rows through the stage's own declaration, requires the receipt's cells to equal it and each cell's design-weighted total after the rake to equal its published value (the Northern Ireland cell jointly over bus and rail), and fails closed on a skipped cell; the #890 rake had no fence. A declared `record_support_per_journey` step (`uk_runtime/bus_support_per_journey.py`) records, per residence area where DfT publishes the components (London, England outside London), what pricing support from the frame's journeys would give: the BUS05 concessionary travel reimbursement per concessionary boarding times the frame's eligible boardings plus the rest of the net support per boarding times every boarding, with boardings the `nts_bus_travel` trips times the fare pricing's boardings per resident trip, beside the raked column's design-weighted total. It is a receipt (`support_per_journey`, `applied: false`), never a value: retiring the support rake is María's ruling with those numbers in hand. `dft_bus_journeys.json` names its two new consumers.
