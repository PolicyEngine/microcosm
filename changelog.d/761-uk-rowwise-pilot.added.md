The UK rowwise pilot machinery (#761, #495 increment 5) lands: the local-areas line is renamed `uk-local-*` and its `uk/local` Logbook scope is ratified (migration + mirror + README together); the rowwise dataset driver gains fail-closed `--input-sha256` and `--ladder-sha256` pins with a committed-crosswalk cross-check; the pool-only source-lineage modulus is refused on spine inputs carrying explicit lineage columns, which now report their own additive lineage block at pre-clone grain; the ladder clone path refuses pre-assigned geography columns; `expected_uk_ladder_area_support` computes the exact analytic per-constituency and per-LA expectation from the sampler's own stage weights; the per-area support diagnostics (ESS, distinct source households) lift off the solve matrix into `uk_area_support_summary` / `uk_ladder_area_support_summary` plus `uk_region_mix`, wired into the ladder manifest, dry-run plan, and a new `area_support_summary.csv` sidecar; and `--candidate-clone-counts` plans rows, bytes, and support at every candidate K in one dry-run.
