A UK publication line carries one dataset role: the publisher refuses a line promotion whose manifest declares another role (`national` publishes `national_default` releases, `local-k<N>` lines `non_default_local_area`), and the loader refuses a manifest that reached a line pointer with the wrong role. The release-line grammar refuses `evidence` (it would collide with the evidence-tier pointer), the loader states why every artifact must pin the pointer's revision, and the national assembler again binds `certification.candidate.filename` to the line's dataset filename.
