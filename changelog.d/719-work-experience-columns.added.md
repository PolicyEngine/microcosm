US release person records carry `detailed_industry_recode` (CPS `WEIND`), `major_industry_recode` (`WEMIND`) and `worked_last_year` (`WKSWORK > 0`) next to `detailed_occupation_recode`: ASEC vintages restore `WEIND`/`WEMIND` from their pinned Census person members before pooling, ACS-spine people get all three from native `INDP`/`WKWN` through the release predictor join (crosswalk version 2), and the `org_wages` carry emits them with the industry identities checked (#719).
