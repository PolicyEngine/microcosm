`tools/assemble_uk_dense_release_dir.py` now requires the run manifest's `staging_delivery` receipt and copies it into `build_manifest.json` as `staging`, the national assembler's rule, so `microcosm-publish-release` applies its undelivered-staging refusal to dense cuts; `--allow-missing-staging` assembles a run built before the staging lane with a recorded disabled-staging opt-out.
