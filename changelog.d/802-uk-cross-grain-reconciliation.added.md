Add a country-agnostic cross-grain target reconciliation pass and the UK standing rule that gives country precedence over constituency and local-authority targets, with exact measurement-signature detection, declared household and UC bridges, fail-closed geography-leg rescaling, and mass-log-style receipts in every UK rowwise dry-run plan and manifest. The current candidate binds no national targets, so its target values remain numerically unchanged; the census disclosure-control fence and adjudication now state how a future same-concept national control governs published constituency values.
