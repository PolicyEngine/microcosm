Publish the reported-receipt child of the US national default as its own `source_enrichment` release (`add_reported_receipt_inputs`, pinned to `populace-us-2024-spm-20260915`), so the ACS local chain can stage from a published donor with `--donor-release-manifest` (#978). A new `tools/build_us_receipt_enrichment_release.py` packages the donor receipt qualification's child. The release contract replays the qualification's exhaustive H5 verifier, which now lives in `microcosm.data.h5_boolean_append`, and qualifies the role plus the three receipts through the native loaders. The publisher refuses to point `latest.json` at this child. The SPM-role lane is unchanged.
