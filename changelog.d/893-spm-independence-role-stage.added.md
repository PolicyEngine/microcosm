Add the `spm_independence_role` US build stage: restore the engine's one declared dataset source input, `is_spm_independent_minor_role`, on a base built from raw sources by exact income-year/PERIDNUM identity from the pinned complete Census ASEC person files through the certified `derive_spm_role_source`, fail closed on a missing source column, a unit with more or fewer than one SPM head, an unmatched person, or a unit left without a classified adult, and classify the role as an input leaf in both engine-adapter paths from `policyengine_us.spm.DATASET_SOURCE_INPUTS` so the export persists it.
