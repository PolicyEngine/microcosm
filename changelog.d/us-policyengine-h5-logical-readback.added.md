Add an opt-in PolicyEngine-US H5 export wrapper that compares persisted values,
integer IDs and memberships, row order, household weights and period with a
supplied retained Frame and common-parent contract. It preserves adapter defaults
and admits only complete SPM roles, explicit scope and complete-input codec
behavior; the receipt does not establish
source ancestry, calibration validity or release eligibility.
