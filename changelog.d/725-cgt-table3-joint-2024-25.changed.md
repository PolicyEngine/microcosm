The UK CGT amounts redraw now draws from HMRC Table 3 for 2024-25 (the 2026 release's joint of individuals and gains by size of gain and taxable income, with the taxable-income bands moved onto the GBP 125,140 Personal Allowance taper edge) copied verbatim from the pinned Chronicle feed into `hmrc_cgt_conditioning_facts.json`, instead of the 2025 release's 2023-24 ODS rescaled onto the 2024-25 band levels. The pinned `Table_3_2025_Size_of_gain_by_income.ods` input, its `verify_pinned_cgt_ods` operation, the `cgt_published_fact_surface` artifact and the `--cgt-ods` build argument are retired; both CGT manifests declare a `verify_vendored_fact_resource` operation that checks the resource's recorded feed against `chronicle_feed.json` before a row is read, and the loader refuses a Table 3 row set whose band totals disagree with the Table 2.1a bands folded onto them beyond publication rounding.
