Reviewed-exclusion registers move to schema 2 (#610): every admitted exclusion is a complete approval receipt — reason, approver, adjudication reference, approval date, and expiry — validated strictly at load, sealed whole into the pinned policy digest, and enforced at gate evaluation with an injected clock: an entry is honored through its `expires_on` and afterwards stops suppressing, failing with a renew-or-remove message naming the approver and adjudication. All three UK registers upgrade together; the shared US-consumed gate layer and the contract's flat details pin are untouched via record-to-reason projection in the UK wrappers, and published reports must carry empty `expired_exclusions`. Resolves the `TODO(PR #610 review)` in `weighted_integrity.py`.
