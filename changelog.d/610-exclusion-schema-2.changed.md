Reviewed-exclusion registers move to schema 2 (#610): every admitted exclusion is a complete approval receipt — reason, approver, adjudication reference, approval date, and expiry — validated strictly at load, sealed whole into the pinned policy digest, and enforced at gate evaluation with an injected clock: an entry suppresses from its `approved_on` through its `expires_on`, and outside that window every gate fails with receipt context at any column state — expired, not-yet-in-force, dormant, or signal-regained — so a register cannot rot silently. Receipt dates must be canonical `YYYY-MM-DD` (two spellings of one date must not mint two policy digests), the policy of record is a lazily-loaded immutable mapping, and the terminal report seals the register snapshot it actually ran under. All three UK registers upgrade together; the shared US-consumed gate layer and the contract's flat details pin are untouched via record-to-reason projection in the UK wrappers, and published reports must carry empty `expired_exclusions`. Resolves the `TODO(PR #610 review)` in `weighted_integrity.py`.
