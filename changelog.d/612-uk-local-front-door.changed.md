UK local solve through the public `microcosm.calibrate` front door (#612
increment 2). The pre-ladder stacked `areas x households` research harness is
removed — `solve_stacked_local_weights`, the stacked doctrine route,
`local_runner`'s candidate build path, and the stacked matrix/long-weights
helpers in `local_geography` (the rowwise clone path is the single UK
local-solve story; positional-assignment safety rides on the Frame kernel's
sorted-group-id invariants). `local_geography` keeps `align_area_targets`;
the doctrine module keeps the reviewed bounds; the uniform-target-surface
check moved beside its rowwise consumer. The ladder-clone result now carries
a microcosm `Frame` (typed household weights, mass log, `time_period`
metadata) with per-entity `{entity}_clone_index` columns in memory; the
written artifact keeps the legacy single-year schema — one `clone_index`
name per table, renamed at the export boundary — so nothing the UK engine
reads changes.
