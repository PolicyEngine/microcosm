Decompose the US head-to-head replacement scorecard on a national/state/congressional-district view. Every fiscal row now carries the comparison view its declared target evidence names, each artifact payload and the head-to-head comparison roll up on that axis, and a row no declared evidence places is reported `unresolved` rather than counted as national. This is the previously unproduced `national_and_cd_target_fit` item of the native survey release-evidence list; it adds no threshold, tolerance or verdict.
