US releases now refuse a stored model input the certified engine does not define (microcosm#1026). policyengine-us ignores a stored column it has no variable for, so `populace-us-2024-spm-20260915` and its reported-receipt child, both certified against policyengine-us 2.2.1, shipped the WIC take-up draw as `would_claim_wic` (renamed `takes_up_wic_if_eligible`) and every WIC-eligible person took WIC up. `microcosm.data.stored_inputs` refuses any stored column that is lowercase snake_case (the engine's naming convention, measured against 2.2.1), is not a variable of the certified engine, and is not in `US_STORED_NON_VARIABLE_COLUMNS`, a reviewed register of 30 provenance and build-construction columns, each with its reason. Every refused column is named with its tables and both remedies: rename it to its live input, or add a reviewed register entry. Two seams run it with the engine the release is certified against. The US fiscal-refresh release tool runs it as a batched pre-export gate and checks that the written H5 earns the same verdict. The source-enrichment native-loader probe runs it before either loader, both at certification and when validation or the publisher replays the contract; the probe's receipt records the register's digest. On the published default the check refuses `would_claim_wic` and `medicare_part_b_premiums`, an ASEC Part B premium transfer target under a name no engine version defines, which #590 already dropped from the build. The Route A rehearsal export built from main passes. `python -m microcosm.data.stored_inputs <h5>...` prints the verdict for local files from HDF metadata alone.
