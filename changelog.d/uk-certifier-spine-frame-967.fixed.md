`tools/certify_uk_release_cut.py` pins the spine (`--spine-sha256`, a Logbook pin role) and loads its frame, and the release-cut battery hands it to `uk_release_input_coverage` as the evidence for the stage families' build state: typed `importance` weights and each stage's mass receipt are a spine-stage contract, and the first release-cut run evaluated them on the calibrated candidate, where the weights are `calibrated` by construction, failing all thirteen families. The coverage and effective-mass halves still read the release frame; the gate's details name which frame the build state came from.\n