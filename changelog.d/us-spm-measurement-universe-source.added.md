Add `us_runtime.spm_universe_source`, the source-owned producer for policyengine-us#9462's `spm_unit_spm_universe_status`: ACS housing units and every ASEC record are `INCLUDED`, ACS group quarters are `OUTSIDE`, and twelve named refusals fail closed rather than guess. Declared from the source's own record type, never from a unit's composition. Not wired into any build path and not exported.
