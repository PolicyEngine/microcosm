Split Logbook lineage by chain scope on both sides — archives at `logbook/us.jsonl` plus `logbook/<country>/<dataset>.jsonl` and a live-store migration deriving the scope from the hashed pipeline name — with a closed-world ratified vocabulary (`us`, `uk/frs`), scope-checked local and remote export, and the UK pipelines renamed onto the base-data convention before any UK row exists.
