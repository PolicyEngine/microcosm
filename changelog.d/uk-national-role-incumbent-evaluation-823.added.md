The rowwise driver's national role evaluates the finished candidate against an incumbent (`--incumbent-h5`, `--incumbent-sha256`, `--incumbent-label`) at the end of the build, after the bundle is staged and before the telemetry completes: `score_vs_incumbent.json` beside the outputs and as a reviewed telemetry artifact, rows the incumbent cannot materialize pruned from both arms and named on stderr, and a manifest `evaluation` block with the rule-1 verdict the release-cut certifier requires. The evaluation never blocks the build; an error is recorded and warned. The full compiled register is frozen beside the solve register as `national_contract_registry.json`. The dense role refuses the flags.\n