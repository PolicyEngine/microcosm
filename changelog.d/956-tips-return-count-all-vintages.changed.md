Enforce the #451 W-2 Box 7 tips return-count exclusion at every tax year (decision d179, microcosm#956). The ty2023 entry joins `US_FISCAL_TARGET_ALL_VINTAGE_SUPPORT_EXCLUSIONS`, so the ty2020 fact, which carries the same 6,038,613 returns and had kept calibrating, no longer does. The July feed carried only the ty2020 vintage, so the ty2023-keyed exclusion matched nothing; on the pinned feed, which carries both, latest-vintage fallback selected ty2020 and #1016 kept it as the one reviewed vintage bypass. `US_FISCAL_TARGET_EXCLUSION_VINTAGE_BYPASSES` is now empty; the mechanism stays. On the pinned feed the `national_state` surface goes from 5,695 targets (registry `386fac439e77`) to 5,694 (`d315c75804ef`).
