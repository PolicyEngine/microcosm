Re-pin the US Chronicle feed to a labelled export at Chronicle `c5e5bf8`, rebuilt reproducibly from 62 targeted package runs (`tools/build_us_chronicle_feed.py`, `us/chronicle_feed_scope.json`, `us/chronicle_feed.json`, `load_us_chronicle_feed`), regenerate the target-parity resources against it with the family surface unchanged (every previously pinned cell keeps its value; 1,759 cells added in existing record sets), and give a substituted Medicaid enrollment spec its own calibration hierarchy so the labelled feed compiles.
