Add `build_native_survey_release`, the in-process native US release entry. It authenticates a live issued enrichment owner before anything else, refuses every option it does not consume, compares the consumer with an expected identity (including re-hashed installed files, the origin of loaded consumer modules and effective SPM settings), projects the declared cells and runs a pure input gate. Only then does it materialize targets with the admitted constructors, run the shared dense/L0 solve, attach weights to the projected inputs, evaluate the fit gates and write one verified logical H5. Outputs are created relative to directory descriptors that follow no links, and the manifest is written once into the directory the build created. Its manifest is never release-eligible and lists outstanding qualifications. Wrong-parent H5 export tests refuse before the writer.
