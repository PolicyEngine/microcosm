UK national build stages now run `Frame -> Frame` on the microcosm kernel carrier. Retired: `UKNationalDataset`, `load_uk_national_dataset`, `write_uk_national_dataset`, `validate_uk_national_dataset`, the result fields `UKNationalBuildResult.dataset`, `UKFRSHMRCRetainedLeavesResult.dataset`, and `UKHMRCIncomeRestorationResult.dataset`. Replacements: `uk_national_frame`, `load_uk_national_frame` (returning the frame plus a `UKStagingProvenance` record carried beside it), `write_uk_national_frame`, `validate_uk_national_frame`, and `.frame`/`.provenance` result fields; `restore_uk_hmrc_income_family` gains a `staging_provenance` parameter, and the SPI stage's certified-candidate fence binds the load provenance and the loaded frame through a single-use `bind_staging_provenance`. Loading now enforces Frame's invariants the shadow carrier never checked — group ids sorted ascending, membership equality in both directions, no reserved `{entity}_weight` columns beyond the exported household one — so artifacts violating them fail closed at load. The staging H5 payload (keys, column order, values, root attrs) is unchanged for conforming artifacts, with one declared canonicalization: the exported `household_weight` column is written from the kernel's typed weights and is therefore always float64 (a non-float64 incumbent widens on its first rewrite). The rowwise clone keeps its own carrier — its artifact is not a national frame (`clone_index` on every entity table violates Frame's column-uniqueness rule).
