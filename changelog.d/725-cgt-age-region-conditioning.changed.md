The UK CGT amounts redraw (`hmrc_cgt_gains` and its spine projection) now conditions on age and household region as well as the taxable-income proxy: the allocation targets are raked to the Table 6 age and Table 5 region margins (the latter on the individuals basis via the Table 1 share) from the vendored `hmrc_cgt_conditioning_facts.json`, and gainers are walked inside income x age x region cells with a pooled per-income-band fallback where a cell lacks support. The stage summary reports achieved against published at the Table 6 and Table 5 grain and the age x gain-band joint microcosm#725 measured, plus the rake and fallback receipts. The rake also holds Table 6 gains by age as one linear constraint per age group (an exponential tilt of the group's cells that keeps its count), so each age group carries its published gains as well as its taxpayers, and the youngest group is 16-24 on its own (seven age groups) so its rows are no longer ranked under the 25-34 carriers.
