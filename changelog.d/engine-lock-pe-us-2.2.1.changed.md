Move the US engine lock to policyengine-us 2.2.1, spm-calculator 1.0.0 and policyengine-core 3.32.5, the trio inside the certified SPM 1.0 claim, and re-derive every fail-closed pin through its own generator: the frame adapter's generated-variable audit (which now also covers spm-calculator's `build_policyengine_variables` surface), the US engine ABI lock and multispine pool engine contracts, the take-up contract's asserted engine, the release input-coverage manifest, the frozen generation-0 resource digests and the spec-engine identity pins. Because policyengine-us 2.0.0 stopped inferring SPM geography from an absent county, every microcosm call site that can reach an SPM measurement variable now names its selection explicitly through the engine's `spm` configuration rather than inheriting a default: the release gate declares `{"geography_kind": "county"}` over the release H5's observed county FIPS, and the frame adapter accepts a selection so a stage running before geography assignment can ask for national measurement instead of raising. Measured against 2.2.1, `household_net_income`, `household_benefits` and marginal tax rates no longer need a county even for housing-assisted units, so only the SPM measurement chain is affected. The county ladder gates now reject a non-text `county_fips` column, which the engine refuses however it is spelled and which the previous `astype(str)` width check silently accepted for codes without a leading zero.
