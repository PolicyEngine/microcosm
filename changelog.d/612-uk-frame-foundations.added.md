Promote the engine-tables materializer out of the two engine adapters into a shared `microcosm.frame.engine_tables` (typed weights authoritative, any existing `{entity}_weight` column overwritten in place), and add `tools/preflight_uk_frame_linkage.py`, an SDC-floor-enforcing classifier that opens audited UK H5 inputs read-only, refuses JSON outputs that alias any input, and checks those artifacts against Frame's linkage invariants ahead of the #612 carrier swap.
