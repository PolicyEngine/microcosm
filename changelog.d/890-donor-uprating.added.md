Declare the uprating of the UK imputation donors to the FRS 2024-25 base year as a manifest operation, `uprate_donor_columns`, on `lcfs_consumption` (the 2023-24 diary) and `etb_services` (the FYE-2024 ETB), replacing the stale "identity at this vintage" note. Each column names its basis: `engine_parameter` reads the ratio of a PolicyEngine UK index at the two January-first instants (the OBR CPI for consumption columns and bus fares, average earnings, mixed income and the private-pension index for the income predictors, lockstepped with what the engine applies afterwards); `vendored_litre_proxy` moves petrol and diesel by the published outturn (DESNZ pump-price ratio times HMRC litres per head from the vendored Chronicle facts) and records the engine's own litre-proxy index ratio beside it as an audit, since the two disagree; `vendored_ratio` moves rail and bus support by ORR 7270 and DfT BUS05bi England net support at the two fiscal starts. The energy columns are exempt (the NEED rake and Ofgem pricing set their level). `uk_runtime/donor_uprating.py` resolves the factors and returns a receipt the stages record as `donor_uprating` evidence; `ledger_fact_vendoring.vendored_rows` is the one fail-closed reader of vendored rows (pin check, fiscal-start and dimension selection) and the BUS0415 alignment now reads through it. The ETB `compute_ratio` denominator moves to `rail_fare_index_2024` (1.165) so `rail_usage` is stated in the uprated donor's own year; the `cpi` block of `lcfs_consumption_anchors.json` retires. Four operation kinds (`uprate_donor_columns`, `assign_bus_use_incidence`, `rake_to_vendored_facts`, `price_energy_at_cap`) join the manifest vocabulary and schema for the rest of microcosm#890 PR-S.
