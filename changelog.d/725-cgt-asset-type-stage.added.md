Add the `hmrc_cgt_asset_type_spine` UK spine stage after the CGT amounts redraw. Every liable gainer (net gains above the annual exempt amount) carries a main asset type in the new person column `capital_gains_asset_type` and, where flagged as a residential property disposer, its net gain in `capital_gains_residential_property`. The residential flag is a logistic in log gains solved so the household-weighted count and gains of flagged persons equal HMRC Table 8a 2024-25 restated on the individuals basis by the Table 8b individuals/all share, realised by seeded systematic sampling; the non-residential remainder draws one of the five Table 7 asset types from a size-tilted categorical whose type weights reproduce the fenced Table 7 2023-24 gains shares (diagnostic only). The rows come from the new vendored `hmrc_cgt_asset_type_facts.json`; the stage declares its operations in the manifests, a stage-health gate holds the realised residential totals within 5 percent, an enum-domain gate holds the column to the declared values, and the graph parity fixture carries synthetic facts sized to its frame.
