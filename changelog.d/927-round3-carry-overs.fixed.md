The items Vahid's third pass on PR #927 left open are closed (microcosm#930 C0). The lcfs stage notes and their packaged mirror no longer say the FY2024-25 Ofgem pricing sets the energy columns' level (the NEED 2024 shape at the DESNZ level and the QEP average prices paid do). The `price_domestic_energy` schema branch now requires the connection keys whenever `gas_connected` is `published_meter_share` (the first `if`/`then` in `sources.schema.json`), and the regions the crosswalk maps to no subnational area are declared on the stage as `connection_fallback_regions` (Northern Ireland) and refused at runtime if they differ from the crosswalk, so the fallback is no longer inferred from a `None`. `iterative_proportional_fit` records the cross-margin residual after every sweep in `raking_sweep_residuals`, the energy rake receipt carries it per fuel as `sweep_residuals`, and the `uk_stage_lcfs_consumption_energy_rake` note states the tolerance governance (a rule above the observed maxima; a converged residual above it is a new ruling) and the connection-cap rule (declared, waived only for a receipted shortfall). The gas-connection walk is documented as the greedy weight-fitting walk it is, with a test on unequal weights that exercises the skip and nearest-skipped branches and `rows_skipped_for_weight`; the `evidence_absent_blocks` plus `synthetic_smoke` seam has a test (an absent receipt blocks the smoke, a failed population fact check does not). Receipts Parts R and S credit the UK spec-digest move to the rebased `713b5064`, Part T records that ONS 04.5.3 and 04.5.4 (about GBP 1.57bn) are vendored and unbound and that María confirmed the 04.5 sign-out, and the merged PR #927 body carries the rebased hashes. The gate-battery and certification-part digests are re-pinned.
