State what the UK `uk_target_surface` gate proves — the solve's realized
target surface equals the declared register at name-and-period grain — rather
than claiming an incumbent comparison the frozen parity instrument cannot
source. Gate manifest and spec-fingerprint pins recomputed.
