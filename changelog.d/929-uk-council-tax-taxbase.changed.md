Re-pin the UK Chronicle consumer feed to `df35af7` (#929, from `474a0ae`): the rows are `chronicle.consumer_fact.v3` (chronicle #267), which carries the publisher's `geography.name` on every fact, so the local target surface compiles again on the schema-8 hierarchy without a catalog entry per constituency or authority (closes #920); `build.chronicle_epoch` declares the v3 identity beside v2. The artifact also carries chronicle #264's council taxbase packages (MHCLG CTB lines by band for England's 296 billing authorities, StatsWales CT1 dwellings by band A–I for Wales, CTAXBASE chargeable dwellings for Scotland's 32 councils; 266,390 rows, +124,990). No existing reference on either surface resolves a different fact or value: the national and local registers regenerate with only the feed identity, the hierarchy catalog header and the fact-supplied geography names moving; the nine vendored resources and the local census restate the pin.

Re-base the UK council tax stock family on the councils' taxbase returns (#929, closes the family's part of #736's council-tax lines). The VOA valuation-list stock counted exempt, empty and second-home dwellings as households (England +6.3 %, Wales +6.6 % over the household rows on the 10 September dense rebuild), the source of the family's uniform negative bias and of 33 of the release-blocking `uk_local_target_fit` failures. Three source-named families replace the 17 `voa.council_tax_stock*` targets on one measurement signature: `mhclg.council_tax_stock.*` binds MHCLG CTB line 7 (chargeable dwellings after the disabled-relief adjustment; band A adds the A- column) minus line 11 (second homes) minus line 15 (empty dwellings) for the 296 English billing authorities, with the nine English region controls composed from the authority rows (a consumer rollup, since MHCLG prints no region row and Chronicle computes no sums); `welshgov.council_tax_stock.*` binds StatsWales CT1 a1 − h7 − h8 by band A–I for the 22 Welsh authorities and the Wales row on the 2025-26 return; `scotgov.council_tax_stock.by_area.*` binds CTAXBASE chargeable dwellings for the 32 councils beside the existing country rows. New shared machinery: a `linear_combination` value operation (signed, n-ary, each operand a sum over its selector with `expected_member_count`, formula stamped in `ledger_value_formula`); geography fan-out cells composed from lower-grain member facts (`composed_from_level`, the hierarchy labels them from the geography catalog); per-target area rosters (`area_scope`) for publications that cover one nation; and publisher code aliases on the crosswalk (`code_aliases`: Barnsley and Sheffield, recoded E08000038/E08000039 in the April 2025 LAD layer, bind their 2025 MHCLG and PIPR rows under the roster codes). Band I joins the local metric roster (Wales only); the empty-leg licences derived from the local membership honour each target's `area_scope`, so a nation's authority family is licensed outright on the other nations' legs. National surface 595 → 605 references; local surface 20,430 → 20,885 (176 Welsh A–H, 22 Welsh I, 255 Scottish, 2 PIPR cells), the 296 English band-H cells still deferred on spine support. Retired: `council_tax_voa_scotland_absent`, `council_tax_ni_domestic_rates`, `council_tax_wales_country_control_absent` (its premise was stale: Chronicle carried the Wales row in every pin), `council_tax_city_of_london_band_a_suppressed`; the `voa_dwellings_vs_household_frame` fence is re-adjudicated onto the taxbase basis. The 55k report's "frozen vs recomputed +3 to +12 %" on the VOA rows was a comparison defect (`frozen_vs_recomputed` summed the Wales rollup into the England-pinned target); it now keys on bound rows only and refuses duplicates.
