Re-pin the UK Chronicle consumer feed to `df35af7` (#929, from `474a0ae`): the rows are `chronicle.consumer_fact.v3` (chronicle #267), which carries the publisher's `geography.name` on every fact, so the local target surface compiles again on the schema-8 hierarchy without a catalog entry per constituency or authority (closes #920); `build.chronicle_epoch` declares the v3 identity beside v2. The artifact also carries chronicle #264's council taxbase packages (MHCLG CTB lines by band for England's 296 billing authorities, StatsWales CT1 dwellings by band A–I for Wales, CTAXBASE chargeable dwellings for Scotland's 32 councils; 266,390 rows, +124,990). No existing reference on either surface resolves a different fact or value: the national and local registers regenerate with only the feed identity, the hierarchy catalog header and the fact-supplied geography names moving; the nine vendored resources and the local census restate the pin.
