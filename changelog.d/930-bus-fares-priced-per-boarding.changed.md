`bus_fare_spending` is no longer level-set by the lcfs stage. The #890 take-up incidence draw (`assign_bus_use_incidence`) and the fare rake to the DfT, Scottish and Translink receipts (`rake_to_vendored_facts` on `bus_fare_spending`) leave the lcfs declaration; a declared `price_bus_journeys` step (`uk_runtime/bus_fare_pricing.py`, microcosm#930) sets the column as the sum over the household's non-eligible persons of the `nts_bus_travel` stage's journeys times boardings per resident trip (the vendored BUS01 journeys over the vendored NTS0705a trips per resident times the ONS population) times the yield per fare-paying boarding (BUS05ai receipts over journeys less concessionary journeys), per area: bus-in-London trips take the London area's price for every resident, other-local-bus trips the price of the residence area (England outside London; Scotland and Northern Ireland from their own receipts and journeys with England's trip rate as the declared proxy). Wales is unpriced and keeps the chain's raw draw clipped to donor support (María's ruling, 2026-09-17). The chain still draws `bus_fare_spending` so later targets condition on the same raw draw; the emitted column is the priced value. The receipt carries every published input with its source records, the derived factors, the frame-implied boardings beside the published ones and the frame's eligible trip share beside the publisher's concessionary boarding share, with the algebra stated: the design-weight fit of the bound receipts targets is a survey-versus-survey composition check times a survey-versus-admin concession check, never a level the stage set. A stage-time `bus_pricing` gate recomputes every factor from the vendored rows (`uk_stage_lcfs_consumption_bus_pricing`, a population fact check); `dft_bus_journeys.json` gains its consumers and `nts_bus_use_frequency.json` loses the lcfs stage.
