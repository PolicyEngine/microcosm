`tools/build_uk_rowwise_candidate.py --release-role national` now builds the certified national line through the calibration seam library: no cloning, national targets only, the seam doctrine (1,500 epochs, `family_equal`, learning rate 0.02, seed 0) with explicit flags as receipted overrides, the six calibration-seam gates, the seam-shaped `build_record.json` that `tools/certify_uk_release_cut.py` certifies, a frozen `national_target_registry.json` for the scorer, and the same staging telemetry (operation `uk_national_calibration`, run id = the attempt id) and staged bundle as the dense role. `run_uk_calibration` accepts a caller-minted attempt id and the committed Chronicle feed-pin check moved to `uk_runtime.chronicle_feed`.
