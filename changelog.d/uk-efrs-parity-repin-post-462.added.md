Add `tools/repin_uk_efrs_parity_reference.py`, one reviewed command that moves the UK enhanced-FRS parity reference to another uk-data release: it resolves the release tag to its Hugging Face commit and the artifact's LFS sha256/size, verifies the licensed bytes, rewrites every committed mirror of the identity in lockstep (the parity tool's pins, `uk/gates.json`'s `efrs-post-calibration` registry entry, `uk_runtime/weighted_integrity.py`, the data-shard contract, and the four test modules that mirror them) and refuses leftovers, regenerates `uk/efrs_parity_reference.json` and `uk/release_input_coverage_manifest.json` with the committed tools, emits the gate's weighted-totals sidecar outside the repository (#609), moves `totals_sha256` (the sidecar's canonical evidence digest), re-cuts the gate-battery policy / manifest / fingerprint / input-mass-evidence digests, writes a disclosure-safe receipt, and runs the lockstep tests; `--dry-run` computes everything into a scratch directory and edits nothing. A new lockstep test (`test_uk_efrs_repin_tool.py`) holds every identity, totals-digest and gate-battery-digest mirror equal to the committed parity reference and proves the recompute path reproduces the committed pins, so a drift in any one mirror now fails PR CI. Prepares the post-uk-data#462 re-pin (v1.56.14 → v1.56.16 or later) for #686.
