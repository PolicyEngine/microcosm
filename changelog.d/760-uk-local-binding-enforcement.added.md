UK rowwise local binding now enforces the 7 reviewed census binding fences at execution time: the doctrine solve derives bound families structurally from the target surface, refuses declared-vs-derived mismatches, checks those families against the committed schema-2 `local_binding_adjudications.json` register with correct-or-renew approval windows, and echoes the adjudications in both the dry-run plan and candidate manifest; the first shipped adjudication accepts census disclosure-control noise for household counts and names constituency as the winning grain for the rowwise arm. Both expiring adjudication registers — the local binding register and the national calibration measure exclusions — now warn when an in-force entry is within one week of its expiry date, so renewal is signalled before the correct-or-renew failure lands.
