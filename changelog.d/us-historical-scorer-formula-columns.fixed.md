Make the US head-to-head scorer drop and receipt historical H5 columns that
the current PolicyEngine-US owns through formulas, after refusing artifacts
that lack the input leaves required to recompute them.
