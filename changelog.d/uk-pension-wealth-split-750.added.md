UK WAS wealth stage emits `private_pension_wealth` (WAS private pension wealth less the current-employment defined-benefit component) as its own household output and keeps `corporate_wealth` to the share-like holdings, so the means-tested capital tests stop counting pension rights; spec, support bounds, export allow-list, coverage manifest and gate-battery digests move in lockstep (#750, uk-data#452).
