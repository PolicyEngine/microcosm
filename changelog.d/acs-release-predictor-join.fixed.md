Populate physical ACS rows' CPS-named inputs for the six archived US release donor models through an exact, SHA-pinned 2024 ACS person/household join; enforce reviewed disability, consumed race/Hispanic, occupation, tenure, and SSI-reporter semantics; refuse ambiguous, partial, or conflicting lineage; and receipt ASEC-native, ACS-joined, and remaining-null counts in both release manifests without changing model selection logic or gate thresholds.
