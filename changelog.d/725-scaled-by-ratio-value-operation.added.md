Add the `scaled_by_ratio` value operation for Ledger target references: a published cell is translated by the quotient of two other facts (`base * numerator / denominator`, ratio in (0, 1]), with the base, numerator, denominator, ratio and member fact keys recorded on the compiled spec. It restates HMRC CGT Table 5 country/region cells, which include trusts, on the individuals basis of the national targets using the Table 1 individuals/total share. The runtime candidate prefilter and the authoring compile now keep the facts an operand selector reaches outside the cell's own selector.
