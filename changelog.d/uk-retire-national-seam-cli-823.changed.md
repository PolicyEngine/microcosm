`tools/calibrate_uk_national_dataset.py` is retired: the national line is built by `tools/build_uk_rowwise_candidate.py --release-role national`, which delegates to the same seam library (`run_uk_calibration`) and was shown bit-for-bit equal to the command on the synthetic parity twin (weights, diagnostics rows, build record). The runbooks, the staging operations guide and the README now name the one driver.
