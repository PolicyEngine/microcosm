Add `FittedRegimeGatedQRF.predict_positive_from_uniforms`, a stateless draw of every target conditional on it being positive: the sign gate is bypassed and each row takes the positive-magnitude forest's value at its caller-supplied quantile, so a separate take-up style incidence mechanism can decide who has a target while the fitted model supplies how much (microcosm#890's bus-use incidence). Regimes without a positive forest and released models are refused by name; no model RNG is consumed. `QRFChainStepResult` gains `fitted`, a one-target `FittedRegimeGatedQRF` view of the step's model (predictors = the chained prefix) with its own reproducible draw stream, so a chained stage can re-draw one target from identity-keyed uniforms without refitting. The `fit.qrf` parity pins are re-pinned for the changed implementation hash; `direct.csv` is byte-identical.
Because the seed protocol attests `microcosm.fit.qrf`, the change moves the seed-protocol and seed-map digests (`spec_engine/inventory_coverage.py`, the regenerated `docs/evidence/spec-engine/us-f0-coverage.json`) and every country's `spec_sha256` (AM, BE and UK bundle pins, the minimal-spec loader golden vector and the pool-tool spec binding), re-pinned in the same change as the amendment-20 lane did.
