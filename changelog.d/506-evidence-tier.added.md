Evidence-tier publishing (microcosm#506): `validate_evidence_release_dir` as a structural sibling of the certified contract (same required files plus a mandatory non-empty `known_failures` block with owner issue refs), `publish_release(evidence=True)` moving only the new `latest-evidence.json` pointer (never `latest.json`), `latest_evidence_release` for consumers, and `microcosm-publish-release --evidence`.
