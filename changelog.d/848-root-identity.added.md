Every raw microdata input a country source manifest declares now carries the SHA-256 of the exact file its stage reads plus a `chronicle_artifact` reference to the Chronicle registration that witnesses it, a fail-closed gate refuses to build from bytes that do not match the pin, and the roots that cannot be pinned yet are listed one reviewed row each in a ratcheted `microdata_pins_pending.json`.
