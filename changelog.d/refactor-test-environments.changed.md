Organize tests by engine requirement and country, run every test environment in one workflow, require the UK integration test in the aggregate CI result, and distribute ordinary tests across two pytest workers instead of creating a matrix job for each alphabetical file group.
