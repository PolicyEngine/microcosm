Organize tests by engine requirement and country, run every test environment in one workflow, require the UK integration test in the aggregate CI result, distribute engine-free tests across two pytest workers, and run country-engine tests serially to stay within hosted-runner memory instead of creating a matrix job for each alphabetical file group.
