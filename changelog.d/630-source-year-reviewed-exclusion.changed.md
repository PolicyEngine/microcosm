The `household.source_year` constant release column is a reviewed exclusion (#630): the new committed `uk/degenerate_reviewed_exclusions.json` register is the degenerate-release-surface policy of record (a `None` argument resolves to it; stale entries still fail), the frozen UK policy digest moved with it — the intended tripwire — and the microcosm-data pin is vintage-aware so the published June release keeps attesting its own pre-#630 digest. The driver gains `--degenerate-exclusions` as a review-time override.
