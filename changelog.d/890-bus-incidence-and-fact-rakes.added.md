Add two UK stage operations that bind imputed bus columns to vendored publisher facts (microcosm#890 I and D). `assign_bus_use_incidence` on `lcfs_consumption` decides who uses local buses take-up style: each person takes a frequency-of-use band by an identity-keyed draw from the vendored DfT NTS0313 shares (NTS0621 for people aged 60 and over; the under-60 shares are derived from the two published series and the frame's design-weighted 60-plus share, since DfT publishes no other breakdown), bands are joined by declared labels because the two tables word them differently, a household is a user when any member rides at least once a year, user households the QRF chain drew at zero take the bus step's positive-regime draw at an identity-keyed quantile (`FittedRegimeGatedQRF.predict_positive_from_uniforms` on the chain step's fitted view) and non-users take zero; the chain keeps conditioning on the raw prior draw. `rake_to_vendored_facts` scales imputed columns so that each declared cell's design-weighted total equals one published FY2024-25 value read from a vendored resource: on `lcfs_consumption`, user households' bus fares to DfT BUS05ai receipts for London and England outside London spread across design-weighted person income quintiles in proportion to NTS0705a trips per person, Scottish Transport Statistics passenger revenue for Scotland and Translink passenger receipts for Northern Ireland (Wales publishes no receipts and is untouched); on `etb_services`, bus support to DfT BUS05bi net support for the two England areas, Scottish Transport Statistics all government support, the StatsWales local-authority gross expenditure on concessionary fares plus support to operators, and the NITHC accounts' PSO plus concessionary compensation bound jointly on bus plus rail support for Northern Ireland (`joint_columns`). Raked columns are exempt from the support clip; both stages record incidence and rake receipts (shares, quintile edges, cell targets, before and after totals, factors) as evidence. Both stages declare the vendored resources they read as artifacts and refuse a cell naming any other.
