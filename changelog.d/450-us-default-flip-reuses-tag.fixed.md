Pin the US publish-now, flip-later flow: after a `--tag-only` or `--no-latest` publish, the standard publish of the same release directory reuses the release-id tag and writes only the `latest.json` commit, and refuses a tag that describes another cut. The tag reuse from #966 already covered this path; the new tests fail with the original 409 (`tag exists`) when it is disabled (microcosm#450).
