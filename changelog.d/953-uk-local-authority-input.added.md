Supply the engine's household `local_authority` input from the rowwise geography ladder (#953): every assigned April 2023 ONS local authority code resolves to its policyengine-uk `LocalAuthority` member name through the sha-pinned ONS names-and-codes resource (`local_authority_names.json`, `tools/generate_uk_local_authority_names.py`) and the mechanical key rule with one declared alias; the assignment refuses any code the roster does not carry, the ladder gate reports any row whose input disagrees with its code, and the clone verifies the assigned members against the installed engine, so council tax and the bespoke Council Tax Reduction schemes stop seeing the Maidstone default on Microcosm builds. The input is declared on the export allow-list rather than the coverage manifest, which measures the incumbent surface; the engine pin moves to policyengine-uk 2.100.0, the first release whose `LocalAuthority` enum carries the six April 2023 unitaries, so the roster-wide membership check is green at the pin.
