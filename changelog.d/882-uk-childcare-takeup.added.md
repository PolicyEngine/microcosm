Draw `would_claim_uc_childcare` in the `frs_take_up` stage at one contract rate per family type (single 0.441, couple 0.2682, fitted by `tools/fit_uk_uc_childcare_takeup.py` so the expected paid-UC count of the Universal Credit childcare element equals DWP's calendar-2025 single and couple households), carried on the extra export surface as a net-new engine input that policyengine-uk 2.98.0 reads (#882). The childcare element register row is retired on the twin evidence.
