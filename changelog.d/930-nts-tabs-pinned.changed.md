The `nts_bus_travel` stage now reads the licensed National Travel Survey extract (UKDS SN 5340, 19th edition, November 2025): the Household, Individual and Trip tabs are pinned by size and digest and verified at runtime, and the declared codebook is verified against the deposited lookup tables and the data extract user guide (microcosm#930). Three corrections from that verification: the frequency-of-use question is `OrdBus2Freq_B01ID` (ten codes since 2019, folded onto the seven NTS0313 bands; code -9, not applicable, is a declared non-user because the published shares reproduce only with those persons in the base, and -8 is dropped and receipted); `Age_B01ID` carries 21 bands, so the codebook maps each code to its lower age before the declared edges band donor and recipient alike (the earlier one-to-one assumption would have misaligned the two); and the trip weight is `W5`, which already carries the diary household weight, so a person's counted trips are divided by W2 before the W2-weighted mean, exactly the user guide's 52.14 × Σ(JJXSC × W5) / Σ W2 (`trip_weight_basis: household_and_trip`, a declared and receipted basis). Interview-only households (W2 of zero) leave the donor and are receipted. The disclosure-safe E6 support bounds for the stage's band and trip columns (`uk/nts_bus_travel_support_bounds.json`, outward-rounded) join the terminal `uk_support` gate, and the H2 fixture's synthetic tabs follow the verified codebook.
