Pin the three processed CPS ASEC inputs (income years 2022–2024) to their public Hugging Face mirror by revision, SHA-256 and byte length (`microcosm.build.us_runtime.asec_sources`), fetch them the way the SIPP donor is fetched with every candidate verified, print the builder's arguments with `tools/fetch_us_asec_sources.py`, and let `tools/build_us_puf_support_base.py --asec-h5-sha256 YEAR=SHA256` refuse an input whose bytes differ from its declared digest before any stage runs.
