The US pool now rejects declared inputs without a live PolicyEngine-US consumer
or an explicitly reviewed non-engine consumer, and no longer transfers the
deprecated, redundant `has_marketplace_health_coverage` carrier.
