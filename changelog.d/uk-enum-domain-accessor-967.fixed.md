The UK enum-domain gates (`uk_uc_deduction_combination_enum_domain`, `uk_student_loan_plan_enum_domain`) resolve their domain through a public `enum_domain(column)` accessor carried by both engine adapters, the frame adapter's `PolicyEngineUKEngine` and the release cut's `PolicyEngineUKCoverageEngine`; the first release-cut run failed both gates with an `AttributeError` because the coverage engine lacked the private lookup the evaluator read, and an engine with neither now names itself in the refusal.\n