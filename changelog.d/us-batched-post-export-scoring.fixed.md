The US release's reform-coverage smoke, reform validation and demographics score the written H5 in household batches, bind its SHA-256 to the release manifest, and evaluate each baseline plan in ascending period order. Each reform shares one tax-benefit system across its batch engines. Guards refuse watched population-aggregate formulas in multi-batch passes, watched baseline-branch readers on reform engines, and changes to behavioral-response parameters. Real-engine tests exercise the refusals and compare shipped baseline plans and selected reform totals with whole-file engines; a separate sequential-worker sweep checks every shipped smoke and out-of-sample validation reform, including OBBBA stacks and in-sample fallback simulations. The MD CCS request-order premise is observed with a warning if the upstream error disappears, not asserted to persist. Writer tests pin export-before-scorer ordering and calibrated in-sample JCT estimates. After constructing the export frame, the build drops calibrated target frames; exact-k retains the clean export frame for its receipt's household-count check. Poverty and SPM outcomes remain record-only.
