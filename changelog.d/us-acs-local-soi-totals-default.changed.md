Make SOI `totals` the default target surface of the US ACS local-area chain (`tools/build_us_acs_local_release.py --soi-mode`, previously `full`), keep `--soi-mode full` as the explicit opt-in for the `soi_fiscal_distribution` surface, refuse unknown modes, record the materialized mode in the build manifest (`materialize.soi_mode`) and in the release refresh recipe, and refuse to package a checkpoint that does not record it. The national release is unchanged.
