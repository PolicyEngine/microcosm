"""One-shot extractors used by the F0 US bundle migration tool.

The modules in this package read the constants-era authority and return plain,
deterministic Python values.  They are migration inputs only; runtime bundle
loading never imports this package.
"""
